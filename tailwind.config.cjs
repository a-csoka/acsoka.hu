const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily:{
				Brooklyn: ["Brooklyn"],
				Gilmer: ["Gilmer"]
			}
		}
	},

	plugins: []
};

module.exports = config;
